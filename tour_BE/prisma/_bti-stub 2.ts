// island-bti-type 대체 stub.
// results.ts가 재수출(export ... from)하는 함수만 제공 → 순환참조(→profile-characters) 차단.
// 시드는 BTI 데이터(문항·결과)만 쓰므로 이 함수들의 실제 구현은 불필요.
export function getIslandBtiType(): null {
  return null;
}
export function getIslandBtiTypeSafe(): null {
  return null;
}
